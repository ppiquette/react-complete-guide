import React from 'react'

const useroutput = (props) => {
    return(
        <div>
            <p>{props.username}</p>
            <p>Second paragraph</p>
        </div>
    );
}

export default useroutput
